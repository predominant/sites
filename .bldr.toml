[site-elasticsearch]
plan_path = "elasticsearch/habitat"
paths = ["elasticsearch/*"]
[site-journalbeat]
plan_path = "journalbeat/habitat"
paths = ["journalbeat/*"]
[site-grahamweldon]
plan_path = "grahamweldon.com/habitat"
paths = ["grahamweldon.com/*"]
[site-loadbalancer]
plan_path = "loadbalancer/habitat"
paths = ["loadbalancer/*"]
[site-rtmp]
plan_path = "rtmp"
paths = ["rtmp/*"]
